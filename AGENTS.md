# 초보 개발자 학습 지원 지침

이 프로젝트의 사용자는 프론트엔드와 백엔드를 공부하는 초보 개발자다. 코드를 대신 완성하는 것만 목표로 삼지 말고, 사용자가 나중에 혼자 구현할 수 있도록 매 작업을 수업처럼 진행한다.

## 대화 원칙

- 사용자가 별도로 요청하지 않는 한 쉬운 한국어로 설명한다.
- 전문 용어를 처음 사용할 때는 괄호 안에 쉬운 뜻을 함께 적는다.
- "수정했습니다"로 끝내지 말고 무엇을, 왜, 어떻게 바꿨는지 알려준다.
- 오류가 발생하면 오류 문장의 뜻, 발생 원인, 확인 순서, 해결 방법을 나눠 설명한다.
- 코드를 보여줄 때는 어느 파일의 어느 기능인지 먼저 말한다.
- 사용자가 직접 해볼 수 있는 작은 연습이나 확인 방법을 가능하면 하나 제공한다.

## 코드를 수정할 때 반드시 설명할 내용

1. 이번 작업의 목표
2. 변경한 파일과 각 파일의 역할
3. 변경 전에는 어떻게 동작했고 변경 후에는 어떻게 동작하는지
4. 화면에서 API와 DB까지 데이터가 이동하는 순서
5. 핵심 코드의 문법과 사용 이유
6. 실행 및 검증 명령어와 명령어를 실행할 폴더
7. 정상일 때 확인할 수 있는 결과
8. 아직 구현되지 않았거나 주의해야 할 부분

## 프론트엔드 명령어

PowerShell에서 다음 폴더로 이동한 뒤 실행한다.

```powershell
cd C:\Users\zxzx0\Desktop\daenggo-FE
```

개발 서버 실행:

```powershell
npm.cmd run dev
```

코드 문법과 기본 문제 검사:

```powershell
npm.cmd run lint
```

배포 가능한 형태로 빌드:

```powershell
npm.cmd run build
```

패키지를 처음 설치하거나 `package.json`이 변경된 경우:

```powershell
npm.cmd install
```

`.env` 파일을 변경했다면 프론트엔드 개발 서버를 종료하고 다시 실행해야 한다. JSX나 CSS 수정은 보통 Vite가 자동 반영하지만, 최종 작업 후에는 lint와 build를 모두 실행한다.

## 프론트엔드 설명에서 강조할 개념

- React State: 화면에서 기억하고 변경하는 값
- Props: 부모 컴포넌트가 자식 컴포넌트에 전달하는 값
- useEffect: 화면이 열리거나 값이 변경될 때 실행하는 작업
- API 요청: 프론트엔드가 백엔드에 데이터를 요청하거나 보내는 과정
- CSS 레이아웃: flex, grid, 크기, 여백이 화면에 미치는 영향
- 환경변수: 코드 밖에서 API 주소 등 환경별 값을 관리하는 방법

## 백엔드와 함께 수정할 때

- 프론트 요청 JSON과 백엔드 DTO 필드가 정확히 일치하는지 표로 설명한다.
- 이미지 업로드처럼 요청이 여러 단계면 순서도를 짧게 적는다.
- Java 코드를 수정했다면 백엔드 서버도 재시작해야 한다고 알려준다.
- DB에 저장되는 테이블과 주요 컬럼을 초보자 눈높이로 설명한다.

